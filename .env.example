# Base URL of the whitepaper-gate Cloudflare Worker (see worker/README.md).
# After deploying, Wrangler prints something like:
#   https://latentschema-whitepaper-gate.<your-subdomain>.workers.dev
VITE_WHITEPAPER_API_URL=https://latentschema-whitepaper-gate.<your-subdomain>.workers.dev
